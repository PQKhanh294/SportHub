// Sport Hub - Global JavaScript
// Tailwind utility classes act as primary styling, minimal JS needed.

document.addEventListener("DOMContentLoaded", () => {
    console.log("Sport Hub application initialized.");
});
