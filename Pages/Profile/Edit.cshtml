@page
@model SportHub.Pages.Profile.EditModel
@{
    ViewData["Title"] = "Ch?nh s?a h? s?";
}

<section class="max-w-3xl mx-auto space-y-6">
    <div>
        <h1 class="text-3xl font-black tracking-tight">Ch?nh s?a h? s?</h1>
        <p class="text-slate-500 dark:text-slate-400">C?p nh?t thông tin cá nhân c?a b?n.</p>
    </div>

    <form method="post" class="bg-white dark:bg-slate-900 rounded-xl border border-slate-200 dark:border-slate-800 p-6 space-y-5 shadow-sm">
        <div asp-validation-summary="ModelOnly" class="text-sm text-red-500"></div>

        <div>
            <label class="block text-sm font-semibold mb-2">H? và tên</label>
            <input asp-for="Input.FullName" class="w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-800" />
            <span asp-validation-for="Input.FullName" class="text-xs text-red-500"></span>
        </div>

        <div>
            <label class="block text-sm font-semibold mb-2">S? ?i?n tho?i</label>
            <input asp-for="Input.PhoneNumber" class="w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-800" />
            <span asp-validation-for="Input.PhoneNumber" class="text-xs text-red-500"></span>
        </div>

        <div>
            <label class="block text-sm font-semibold mb-2">URL ?nh ??i di?n</label>
            <input asp-for="Input.AvatarUrl" class="w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-800" placeholder="https://..." />
            <span asp-validation-for="Input.AvatarUrl" class="text-xs text-red-500"></span>
        </div>

        <div>
            <label class="block text-sm font-semibold mb-2">Trình ??</label>
            <select asp-for="Input.SkillLevel" class="w-full rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-800">
                <option value="">Gi? nguyên</option>
                <option>Beginner</option>
                <option>Intermediate</option>
                <option>Advanced</option>
                <option>Professional</option>
            </select>
        </div>

        <div class="flex gap-3">
            <button type="submit" class="px-6 py-2.5 bg-primary text-white rounded-lg font-bold hover:bg-primary/90">L?u thay ??i</button>
            <a asp-page="/Profile/Index" class="px-6 py-2.5 bg-slate-100 dark:bg-slate-800 rounded-lg font-bold">Quay l?i</a>
        </div>
    </form>
</section>
