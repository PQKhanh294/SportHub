@page
@model SportHub.Pages.Settings.IndexModel
@{
    ViewData["Title"] = "Cài đặt";
}

<section class="max-w-3xl mx-auto space-y-8">
    <div>
        <h1 class="text-4xl font-black tracking-tight">Cài đặt</h1>
        <p class="text-slate-500 dark:text-slate-400 mt-2">Tùy chỉnh trải nghiệm của bạn trên Sport Hub.</p>
    </div>

    <div class="grid grid-cols-1 gap-6">
        <!-- Chế độ giao diện -->
        <div class="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6 shadow-sm overflow-hidden relative">
            <div class="flex items-center justify-between mb-6">
                <div>
                    <h3 class="text-lg font-bold">Giao diện</h3>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Chọn chế độ sáng hoặc tối cho ứng dụng.</p>
                </div>
                <span class="material-symbols-outlined text-primary text-3xl">contrast</span>
            </div>

            <div class="grid grid-cols-2 gap-4">
                <form method="post" asp-page-handler="SetTheme">
                    <input type="hidden" name="theme" value="light" />
                    <button type="submit" class="w-full p-4 rounded-xl border-2 transition-all flex flex-col items-center gap-3 @(Model.CurrentTheme == "light" ? "border-primary bg-primary/5 ring-4 ring-primary/10" : "border-slate-100 dark:border-slate-800 hover:border-slate-200")">
                        <div class="size-12 rounded-full bg-amber-100 text-amber-600 flex items-center justify-center">
                            <span class="material-symbols-outlined">light_mode</span>
                        </div>
                        <span class="font-bold">Chế độ sáng</span>
                        @if (Model.CurrentTheme == "light") {
                            <span class="px-2 py-0.5 bg-primary text-white text-[10px] font-bold rounded-full uppercase tracking-wider">Đang sử dụng</span>
                        }
                    </button>
                </form>

                <form method="post" asp-page-handler="SetTheme">
                    <input type="hidden" name="theme" value="dark" />
                    <button type="submit" class="w-full p-4 rounded-xl border-2 transition-all flex flex-col items-center gap-3 @(Model.CurrentTheme == "dark" ? "border-primary bg-primary/5 ring-4 ring-primary/10" : "border-slate-100 dark:border-slate-800 hover:border-slate-200")">
                        <div class="size-12 rounded-full bg-slate-800 text-slate-200 flex items-center justify-center border border-slate-700">
                            <span class="material-symbols-outlined">dark_mode</span>
                        </div>
                        <span class="font-bold">Chế độ tối</span>
                        @if (Model.CurrentTheme == "dark") {
                            <span class="px-2 py-0.5 bg-primary text-white text-[10px] font-bold rounded-full uppercase tracking-wider">Đang sử dụng</span>
                        }
                    </button>
                </form>
            </div>
        </div>

        <!-- Ngôn ngữ -->
        <div class="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6 shadow-sm">
            <div class="flex items-center justify-between mb-6">
                <div>
                    <h3 class="text-lg font-bold">Ngôn ngữ</h3>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Chọn ngôn ngữ hiển thị chính.</p>
                </div>
                <span class="material-symbols-outlined text-primary text-3xl">language</span>
            </div>

            <div class="space-y-3">
                <form method="post" asp-page-handler="SetLanguage">
                    <input type="hidden" name="culture" value="vi-VN" />
                    <button type="submit" class="w-full flex items-center justify-between p-4 rounded-xl border-2 transition-all @(Model.CurrentLanguage == "vi-VN" ? "border-primary bg-primary/5" : "border-slate-100 dark:border-slate-800 hover:border-slate-200")">
                        <div class="flex items-center gap-4">
                            <span class="text-2xl">🇻🇳</span>
                            <div class="text-left">
                                <p class="font-bold">Tiếng Việt</p>
                                <p class="text-xs text-slate-500">Tiếng Việt (Việt Nam)</p>
                            </div>
                        </div>
                        @if (Model.CurrentLanguage == "vi-VN") {
                            <span class="material-symbols-outlined text-primary font-bold">check_circle</span>
                        }
                    </button>
                </form>

                <form method="post" asp-page-handler="SetLanguage">
                    <input type="hidden" name="culture" value="en-US" />
                    <button type="submit" class="w-full flex items-center justify-between p-4 rounded-xl border-2 transition-all @(Model.CurrentLanguage == "en-US" ? "border-primary bg-primary/5" : "border-slate-100 dark:border-slate-800 hover:border-slate-200")">
                        <div class="flex items-center gap-4">
                            <span class="text-2xl">🇺🇸</span>
                            <div class="text-left">
                                <p class="font-bold">English</p>
                                <p class="text-xs text-slate-500">English (United States)</p>
                            </div>
                        </div>
                        @if (Model.CurrentLanguage == "en-US") {
                            <span class="material-symbols-outlined text-primary font-bold">check_circle</span>
                        }
                    </button>
                </form>
            </div>
        </div>
    </div>

    <!-- Thông tin hệ thống -->
    <div class="text-center pt-4">
        <p class="text-xs text-slate-400 font-medium tracking-widest uppercase">Sport Hub v1.0.0 — 2026</p>
    </div>
</section>
